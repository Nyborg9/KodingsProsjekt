@model WebApplication2.Data.GeoChange

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>GeoChange</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.GeoJson)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.GeoJson)
        </dd>
    </dl>

    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="Id" />

        <!-- Add hidden field for returnUrl -->
        <input type="hidden" name="returnUrl" value="@ViewBag.ReturnUrl" /> <!-- Include returnUrl here -->

        <button type="submit" class="btn btn-danger">Delete</button>
        <a href="@ViewBag.ReturnUrl" class="btn btn-secondary">Cancel</a> <!-- Use returnUrl for the cancel link -->
    </form>

</div>
